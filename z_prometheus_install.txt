global:
  scrape_interval: 30s
  evaluation_interval: 25s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
	  
prometheus.yml 

docker volume create prometheus-volume

docker run --rm --detach --name my-prometheus --publish 9090:9090 --volume prometheus-volume:/prometheus --volume C:\Users\user\prometheus.yml:/etc/prometheus/prometheus.yml prom/prometheusdocker volume create prometheus-volume